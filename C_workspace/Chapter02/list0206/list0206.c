#include <stdio.h>
int main(void)
{
    int n;
    double m;

    n = 9.99;
    m = 9.99;
    
    printf("int   型変数 nの値 :%d\n", n);
    printf("int   型変数 n / 2 :%d\n", n / 2);

    printf("double型の変数 nの値 :%f\n", m);
    printf("double型の変数 n / 2 :%f\n", m / 2);

    return 0;
}