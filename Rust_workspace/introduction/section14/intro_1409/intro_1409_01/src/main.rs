use std::ops::Add;
use std::marker::PhantomData;

// 単位を定義するため、空の列挙型を作成。
#[derive(Debug, Clone, Copy)]
enum Inch {}
#[derive(Debug, Clone, Copy)]
enum Mm {}


fn main() {
    println!("Hello, world!");
}
